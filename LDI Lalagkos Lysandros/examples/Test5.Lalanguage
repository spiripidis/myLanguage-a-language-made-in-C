Class pidi
{
  int main()
  {

    int kserwgwkati (int a, int b);
    { 
      if (a>b) 
      { return "happy";
      }
    }

    int i = 2;
    //int j;
    
	/* this is a multiline comment just for 
	the sake of proving that i did it*/


	void multiplication ();
	{
		//this functions is for multiplication
		int a = 2;
		int b = 3; 
		int c;
		c = a*b;
	}
    while(2)
    {
      int j;
      i = 3;
      j = 3 + i / 6 + 2 + 5;

      int x = 6;
      j = ok = 7;

      if(j < 5 && i == 3 || j!= 5)
      {
        j = 2;

        if(2<4)
          return 1;

        else if(2>3)
          return 0;

        else
          i = 4 + 5;
      }
    }    
  }
}